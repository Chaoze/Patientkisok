# Patient Kiosk

Android kiosk app for scanning a patient anamnesis QR code and opening the approved HTTPS page inside a locked-down WebView.

The installable APK is built automatically by GitHub Actions after changes are pushed to `main`.
